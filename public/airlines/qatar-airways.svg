<!-- INTERIM PLACEHOLDER. Qatar Airways ships no usable square mark in the
     asset set, so this is a monogram tile in the carrier's burgundy. Replace
     this file with the real square logo when one is licensed — the slug is
     what the code references, so a swap needs no code change. -->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 96 96" width="96" height="96" role="img">
  <rect width="96" height="96" rx="21" fill="#5C0632"/>
  <text x="48" y="48" fill="#FFFFFF" font-family="system-ui, sans-serif" font-size="34" font-weight="700" letter-spacing="0.5" text-anchor="middle" dominant-baseline="central">QR</text>
</svg>
